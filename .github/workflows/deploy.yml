name: Deploy to EC2

on:
  push:
    branches:
      - master # 브랜치를 master로 변경

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Deploy to EC2
        uses: appleboy/ssh-action@v0.1.0
        with:
          host: 13.124.3.73
          username: ubuntu
          key: ${{ secrets.EC2_PRIVATE_KEY }}
          debug: true # 디버그 활성화
          script: |
            cd /var/www/html
            git pull origin master  # master 브랜치에서 최신 코드 Pull
            cd frontend
            npm install
            npm run build
            cd ../backend
            npm install
            pm2 restart backend-app || pm2 start app.js --name "backend-app"
